
#include<iostream>
using namespace std;
enum DataType{binary,text};

struct FileInfo
{
    string path;
    int startByte;
    int endByte;
};

struct ChunkInfo
{
    int assignedTo;
    int size;
    vector<FileInfo> chunk;
    
};
struct FileSize
{
    string path;
    int size;
};
